# ☁️ 用 Cloudflare 搭建永久免费图床+网盘，彻底告别服务器！

---

## 🔥 项目登场：CloudFlare-ImgBed

> **项目地址**： [CloudFlare-ImgBed](https://github.com/MarSeventh/CloudFlare-ImgBed)

这是一个基于 Cloudflare 的开源项目，本质上它帮你实现：

> **图床 + 文件床 + 轻量网盘 三合一**

而且重点来了——

> **可以做到接近 0 成本运行**

这个项目的核心意义就是：

> **帮你用“0成本”搞定一个长期稳定的图床+网盘系统**

---

## 📺 视频教程

- **YouTube**： [https://youtu.be/zHyE7ZR3-Q8](https://youtu.be/zHyE7ZR3-Q8)
- **哔哩哔哩**： [https://b23.tv/NFepEof](https://b23.tv/NFepEof)

---

## 🛠️ 快速部署指南

### 1. 关联并部署项目
关联你的 GitHub 帐号，选中刚才 Fork 的项目，点击“开始部署”。

### 2. 配置部署选项

| 配置项 | 值 | 说明 |
| :--- | :--- | :--- |
| **项目名称** | `cloudflare-imgbed`（或自定义） | 项目标识符 |
| **生产分支** | `main` | 生产环境分支 |
| **构建命令** | `npm install` | **重要：v2.0 新构建命令** |
| **构建输出目录** | `/frontend-dist` | 前端构建产物目录 |

### 3. 创建并绑定存储服务

- **创建 R2 存储桶**： 进入 R2 页面，新建一个存储桶，命名为 `my-img-bed`。
- **创建 KV 数据库**： 进入 Workers KV，创建一个命名空间，命名为 `img_url`。
- **绑定 KV 数据库**： 回到 Cloudflare Pages 的项目设置，找到「绑定（Bindings）」。
    - 新增一个 KV 绑定，**变量名**填写 `img_url`，绑定目标选择你刚刚创建的 `img_url` 命名空间。
- **绑定 R2 存储桶**： 新增一个 R2 绑定，**变量名**设为 `img_r2`，指向你的存储桶 `my-img-bed`。
- ![11-3.webp](https://cos.kjkkk.ccwu.cc/2026/07/11-3.webp)

### 4. 安全设置（重要）
为了避免你的图床被别人随意使用，可以在环境变量里加一层“安全校验”：

- `AUTH_CODE`：相当于上传时用的密钥，没有这个码就不能上传文件。
- `BASIC_USER` 和 `BASIC_PASS`：用来给后台管理页面加一层登录认证（账号 + 密码）。

> 简单来说，就是添加三个变量，给上传入口和管理后台各上一把锁，防止被滥用。

### 5. 完成部署
配置完成后，点击**重新部署**。

### 6. 绑定域名
最后一步：点击**自定义域**，绑定你自己的域名。

---

## ✅ 完成

搭建完成！现在你已经拥有一套支持拖拽上传、运行稳定、权限可控的专属图床系统，一切数据都掌握在自己手里。

**访问你的域名，输入自定义的密码进入即可。** 🎉